import numpy as np

amplitude, frequency, phaseOffset = np.pi/4, 100, np.pi/3

# amplitude_back, frequency_back, phaseOffset_back = np.pi/4, 6, np.pi/3
# amplitude_front, frequency_front, phaseOffset_front = np.pi/4, 1000, 0

